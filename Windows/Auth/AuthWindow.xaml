<Window x:Class="StudyTracker.Windows.Auth.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudyTracker.Windows.Auth"
        mc:Ignorable="d"
        Title="AuthWindow" Height="200" Width="300"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
            <TextBlock Text="Name"/>
            <TextBox Name="NameBlock"/>
            <TextBlock Text="Password"/>
            <PasswordBox Name="PasswordBlock"/>
        </StackPanel>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <Button  Content="Войти" Click="LoginButton" />
            <Button  Content="Регистрация" Click="RegistrationButton" />
        </StackPanel>
    </Grid>
</Window>
